#include "add.h"

/*
    You also need to define a .cpp file, then include the header file
    and then defining its methods and variables
    Keep in mind that this is only possible to do once, if its modified more than once
    it produces a compile error
*/

int add(int x, int y)
{
    return x + y;
}